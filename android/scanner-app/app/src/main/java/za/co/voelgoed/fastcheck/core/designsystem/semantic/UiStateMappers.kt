/**
 * Shared mapping utilities for semantic UI state.
 *
 * Provides common mapping functions that domain-specific semantic
 * state files use to resolve [StatusTone] into concrete visual
 * attributes (color role, icon, label).
 */
package za.co.voelgoed.fastcheck.core.designsystem.semantic
