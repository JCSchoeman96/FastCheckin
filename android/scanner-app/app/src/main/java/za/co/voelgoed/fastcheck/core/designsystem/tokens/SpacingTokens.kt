/**
 * Spacing scale tokens for FastCheck.
 *
 * A consistent set of Dp spacing values (padding, margins, gaps)
 * derived from the existing layout conventions in the scaffold UI
 * (4dp, 8dp, 12dp, 24dp).
 */
package za.co.voelgoed.fastcheck.core.designsystem.tokens
