/**
 * Semantic UI state for attendee sync status.
 *
 * Maps sync lifecycle states (idle, syncing, synced, error, rate-limited)
 * to [StatusTone] values for visual presentation.
 */
package za.co.voelgoed.fastcheck.core.designsystem.semantic
