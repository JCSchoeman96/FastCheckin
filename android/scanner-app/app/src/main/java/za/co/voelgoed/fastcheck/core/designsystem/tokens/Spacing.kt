/**
 * FastCheck design-system spacing scale.
 *
 * Provides a consistent set of Dp spacing values (padding, margins, gaps)
 * derived from the existing layout conventions observed in the scaffold UI
 * (4dp, 8dp, 12dp, 24dp). Feature screens should reference these tokens
 * rather than using bare Dp literals.
 */
package za.co.voelgoed.fastcheck.core.designsystem.tokens
