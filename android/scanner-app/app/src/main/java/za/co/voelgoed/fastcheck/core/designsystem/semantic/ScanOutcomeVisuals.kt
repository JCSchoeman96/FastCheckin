/**
 * Scan-outcome visual mapping.
 *
 * Bridges domain-level scan and flush outcome models
 * (FlushItemOutcome, CaptureFeedbackState, QueueCreationResult)
 * to semantic color roles and optional icon/label tokens for
 * visual presentation in feature screens.
 *
 * This file is intentionally empty until semantic color tokens
 * are defined and the bridge points are wired.
 */
package za.co.voelgoed.fastcheck.core.designsystem.semantic
