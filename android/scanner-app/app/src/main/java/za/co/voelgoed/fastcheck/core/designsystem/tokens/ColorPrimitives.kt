/**
 * Raw color palette primitives for FastCheck.
 *
 * Brand, neutral, and accent color values defined as raw Color constants.
 * These are not consumed directly by feature screens; they feed into
 * Material3 color-scheme construction and [ColorRoles].
 *
 * Brand red #D12C26 lives here as a brand token, not as the universal
 * destructive/error color.
 */
package za.co.voelgoed.fastcheck.core.designsystem.tokens
