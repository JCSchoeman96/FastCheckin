@file:Suppress("unused")

package za.co.voelgoed.fastcheck.feature.scanning

/**
 * `feature.scanning` is the home for the real scanner capture flow: preview,
 * analyzer, scanner-local state, and scanner UI.
 *
 * `feature.queue` remains the temporary manual/debug queue UI and is not the
 * home for real CameraX/ML Kit capture work.
 */
internal object PackageNote
